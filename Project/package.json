{
  "name": "shopez",
  "version": "1.0.0",
  "description": "ShopEZ - Full Stack MERN E-Commerce Application",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "nodemon server/server.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "seed": "node server/seeder.js",
    "seed:destroy": "node server/seeder.js -d",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "keywords": ["ecommerce", "mern", "react", "nodejs", "mongodb"],
  "author": "ShopEZ Team",
  "license": "MIT"
}
